\section{系统实现}
\label{sec:impl}

第~\ref{sec:design}~章集中讨论了核心的元数据设计和文件操作。但一次完整的用户请求还需要 FUSE 客户端、MetadataServer 与 DataServer 三个组件协同才能完成，本章从系统实现角度对这三部分进行阐述。内容涵盖代码组织与语言选择、客户端到两个服务的请求分发与编排、MetadataServer 与 DataServer 各自的实现、跨服务的 gRPC 协议与连接层优化，以及正常路径之外的错误处理与恢复边界。

\subsection{实现概览}
\label{sec:impl_overview}

RucksFS 的整体架构如图~\ref{fig:architecture} 所示，由 FUSE 客户端、MetadataServer（MDS）、DataServer（DS）三个组件组成，三者之间通过 gRPC 通信。客户端是整条路径的入口，负责接收来自 Linux 内核的 FUSE 请求，通过内部的请求分发层把每类调用转发到 MDS 或 DS；MDS 承担全部元数据操作，包括命名空间管理、inode 状态维护、PCC 事务、DeltaOp 懒折叠等在第~\ref{sec:design}~章详述的内容；DS 承担实际文件数据的读写，内部把每个 inode 映射到磁盘上同一个大文件的固定偏移区段，直接走裸磁盘 I/O，不再经过本地文件系统这一层抽象。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{images/architecture.pdf}
    \caption{RucksFS 系统整体架构}
    \label{fig:architecture}
\end{figure}

这种架构把”命名空间与 inode 语义”和”字节读写”两类职责分开：前者集中在 MDS，后者集中在 DS，跨两者的语义（例如 \texttt{open} 先拿数据路由再走数据路径、\texttt{unlink} 后触发数据清理）由客户端显式编排。这样每个服务内部的状态相对独立，有利于展示本课题的实现，且排查问题时也更容易定位到具体组件。

实现语言选择 Rust，主要基于两点考虑。第一，文件系统作为基础设施对内存安全要求较高，Rust 的所有权与生命周期模型使得并发共享状态的管理可以在编译期完成检查，避免运行时出现内存错误。第二，Rust 的异步生态与 FUSE 的回调模型契合度较好：一次 FUSE 请求往往要跨越 FUSE、gRPC、RocksDB 三层 I/O 边界，协程加阻塞等待的实现方式在这种多阶段 I/O 场景下可以在固定线程资源下维持较高并发度。

\subsection{客户端实现}
\label{sec:impl_client}

客户端由三部分组成：FUSE 协议层直接面向内核，请求分发层把每类调用映射到对应的后端，远端存根负责与 MetadataServer 和 DataServer 的 gRPC 通信。本节先讲 FUSE 层的工作方式和挂载配置，再讲请求如何在两个服务之间分发，最后以 \texttt{open} 为例说明典型的跨服务编排流程。

\subsubsection{FUSE 层与挂载配置}

FUSE 客户端使用 fuse3 0.8 库实现，启用 \texttt{tokio-runtime} 与 \texttt{unprivileged} 两个特性。前者让 FUSE 回调直接运行在 tokio 异步运行时之上，当一次回调需要等待后端 RPC 返回时可以让出协程、让执行器推进其他请求；后者则允许以普通用户身份挂载 FUSE，不依赖 setuid 的 \texttt{fusermount} 帮助程序，便于在受限容器环境里部署。

挂载阶段的两项关键配置是 \texttt{default\_permissions} 和 \texttt{allow\_other}。前者让内核 VFS 在请求进入 FUSE 客户端前完成标准的 owner/group/other 权限检查，客户端拿到的请求都已经过内核过滤，不需要重复实现 POSIX 权限语义；后者让挂载点对其他 uid 可见，便于多用户或多进程访问。目录项与属性缓存的 TTL 由客户端根据场景配置：生产部署下 TTL 通常设为秒级以吸收重复查询；第~\ref{sec:experiments}~章的性能测试把 TTL 设为 0 以测量 MDS 纯路径的吞吐。

\subsubsection{请求分发与缓存}

请求分发层按请求与后端的依赖关系把一次 FUSE 调用分成三类。

第一类是纯元数据操作，包括 \texttt{lookup}、\texttt{create}、\texttt{mkdir}、\texttt{unlink}、\texttt{rmdir}、\texttt{rename}、\texttt{readdir}、\texttt{getattr}、\texttt{setattr}。这些调用只涉及命名空间和 inode 状态，直接转发给 MetadataServer 即可。

第二类是纯数据操作，包括 \texttt{read}、\texttt{write}、\texttt{truncate}、\texttt{fsync}。这些调用需要知道目标 inode 的数据落在哪一台 DataServer 上，但这类路由信息在 \texttt{open} 阶段已经获取并缓存在文件句柄里，客户端不必每次 I/O 都问 MetadataServer，直接按句柄中记录的服务器编号路由即可。

第三类是跨服务的复合操作，其特点是一次调用需要先后触及两个服务。例如 \texttt{open} 要先从 MetadataServer 拿到 \texttt{DataLocation} 才能用于后续 I/O；\texttt{release} 对应 \texttt{close}，在 \texttt{unlink} 后句柄归零时还需要到 DataServer 侧做数据清理；覆盖式 \texttt{rename} 和带截断的 \texttt{setattr} 则是在元数据事务返回后，客户端再根据响应结构（\texttt{purged\_inodes}、\texttt{needs\_truncate} 等字段）补一个数据侧的清理或截断请求。

这样划分之后，命名空间与 inode 状态的语义约束全部留在 MetadataServer 内、字节读写与清理留在 DataServer 内，跨两者的复合语义由客户端显式编排。边界清楚的直接好处是出问题时更容易定位。代价是客户端比本地文件系统中只做请求转发的客户端更复杂，这是分离式架构必然要承担的一层成本。

\subsubsection{以 \texttt{open} 为例的跨服务编排}

% \begin{figure}[htbp]
%     \centering
%     \begin{tikzpicture}[>=stealth]
%         % lanes
%         \foreach \x/\name in {0/App, 3.2/{FUSE 客户端}, 7.2/MDS, 11.0/DS} {
%             \node[
%                 draw,
%                 rounded corners=2pt,
%                 fill=black!5,
%                 minimum width=2.2cm,
%                 minimum height=0.7cm
%             ] at (\x, 0) {\normalfont\mdseries\small \name};
%             \draw[dashed, gray] (\x, -0.4) -- (\x, -8.6);
%         }

%         % arrows
%         \draw[->, thick] (0, -0.9) --
%             node[above] {\normalfont\mdseries\scriptsize open('/a/b')}
%             (3.2, -0.9);

%         \draw[->, thick] (3.2, -1.5) --
%             node[above] {\normalfont\mdseries\scriptsize Lookup(1, 'a')}
%             (7.2, -1.5);

%         \draw[<-, thick] (3.2, -2.1) --
%             node[above] {\normalfont\mdseries\scriptsize inode, FileAttr}
%             (7.2, -2.1);

%         \draw[->, thick] (3.2, -2.7) --
%             node[above] {\normalfont\mdseries\scriptsize Lookup(17, 'b')}
%             (7.2, -2.7);

%         \draw[<-, thick] (3.2, -3.3) --
%             node[above] {\normalfont\mdseries\scriptsize inode, FileAttr}
%             (7.2, -3.3);

%         \draw[->, thick] (3.2, -3.9) --
%             node[above] {\normalfont\mdseries\scriptsize Open(inode)}
%             (7.2, -3.9);

%         \draw[<-, thick] (3.2, -4.5) --
%             node[above] {\normalfont\mdseries\scriptsize FileHandle + DataLocation}
%             (7.2, -4.5);

%         \node[align=left, text=black!60] at (3.2, -5.3)
%             {\normalfont\mdseries\scriptsize 缓存 $\{fh \to (server\_id, inode)\}$};

%         \draw[<-, thick] (0, -6.1) --
%             node[above] {\normalfont\mdseries\scriptsize fd}
%             (3.2, -6.1);

%         \node[align=center, text=black!60] at (3.2, -7.0)
%             {\normalfont\mdseries\scriptsize \shortstack{后续 read/write\\直接走 DS}};

%         \draw[->, thick, black!50] (3.2, -7.8) --
%             node[above] {\normalfont\mdseries\scriptsize Read/Write(inode, offset)}
%             (11.0, -7.8);
%     \end{tikzpicture}
%     \caption{open 跨服务编排的请求时序}
%     \label{fig:open_seq}
% \end{figure}

% 三类操作中第三类最能体现跨服务编排。以打开一个已存在的文件 \texttt{/a/b} 为例，完整流程见图~\ref{fig:open_seq}。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{images/open_sequence_diagram.png}
    \caption{open 跨服务编排的请求时序}
    \label{fig:open_seq}
\end{figure}



应用发起 \texttt{open("/a/b")} 后，FUSE 客户端需要先把路径解析到目标 inode，具体做法是向 MetadataServer 发两次 \texttt{Lookup}，第一次取 \texttt{a}、第二次取 \texttt{b}，每次都拿回 inode 和完整属性。拿到目标 inode 后再发一次 \texttt{Open}，服务端为此 inode 创建一个文件句柄，并连同对应的 \texttt{DataLocation}（其中包含 DataServer 编号和 inode）一起返回。客户端在本地维护句柄表 $\{fh \to (server\_id, inode)\}$，后续对该句柄的 \texttt{read}/\texttt{write} 直接按 \texttt{server\_id} 路由到 DataServer，不再需要经过 MetadataServer。这样数据 I/O 的稳态路径只涉及一次 gRPC 往返，\texttt{open} 阶段一次性付清的路由代价换来了后续每次 I/O 的快路径。

\subsection{MetadataServer 实现}
\label{sec:impl_mds}

MetadataServer 的职责在第~\ref{sec:design}~章已经讨论过，即维护命名空间与 inode 状态，处理 DeltaOp 折叠，保证并发目录操作正确。本节从工程实现角度补充两件事：异步请求与阻塞事务如何共存、启动时如何完成持久化状态的初始化。

\subsubsection{异步请求与阻塞事务的搭配}

RocksDB 的 Rust 绑定是同步阻塞的。但 RucksFS 的对外接口（gRPC 服务与 FUSE 回调）都在 tokio 异步运行时上。两者结合的做法是：gRPC 请求处理器以 \texttt{async} 形式定义，但实际落到 RocksDB 的事务提交时，先用 \texttt{tokio::task::spawn\_blocking} 把阻塞调用切到阻塞线程池执行，提交完成后再把结果通过 future 交还给原请求。这样异步请求池不会被阻塞调用阻塞住，且事务本身依然运行在专门用于阻塞 I/O 的线程里，CPU 调度上不冲突。

另一处需要注意的是共享状态。MetadataServer 的 RocksDB 句柄、\texttt{InodeFoldedCache}、inode 分配器这些内部状态都用 \texttt{Arc} 共享到请求处理器和后台线程里。缓存分片内部各持一把细粒度互斥锁，在高并发 \texttt{getattr} 场景下锁竞争面积远小于一把全局锁。

\subsubsection{启动与持久化}

MetadataServer 启动时做几件事：初始化 RocksDB 实例，按设计打开 \texttt{inodes}、\texttt{dir\_entries}、\texttt{delta\_entries}、\texttt{system} 四个列族；检查根 inode（编号固定为 1）是否已存在，不存在则写入，以保证每次挂载都能从一致的根目录开始工作；启动 \texttt{DeltaCompactionWorker} 后台线程；最后绑定 gRPC 服务端口对外提供服务。根 inode 编号固定为 1 这一选择带来的副作用是：重启后客户端看到的路径解析入口总是同一个 inode，与上次运行时一致，不会因 inode 分配器内部状态漂移而导致根目录换人。

持久化保证由 RocksDB WAL 提供。所有写事务在提交时会先写 WAL，崩溃后重启时依据 WAL 回放未落盘的 MemTable。尚未提交的事务不会被当成”已经成功”；只要事务返回成功给客户端，对应的修改在磁盘上就一定可恢复。

\subsection{DataServer 实现}
\label{sec:impl_ds}

DataServer 的实现刻意简化，目的是让元数据路径的性能差异不被复杂的块管理细节干扰。对外，它提供 \texttt{Read}、\texttt{Write}、\texttt{Truncate}、\texttt{DeleteData}、\texttt{Flush} 五个 gRPC 接口，分别对应字节读、字节写、截断、删除与刷盘；服务端不维护命名空间、也不参与事务，每个 gRPC 调用直接转发到内部的存储后端。

当前唯一的存储后端是 \texttt{RawDiskDataStore}。所有 inode 的内容共享同一个文件 \texttt{data.raw}，每个 inode 分配一段固定大小的逻辑地址区间，读写偏移按公式
\[
\text{disk\_offset} = \text{inode} \times \text{max\_file\_size} + \text{file\_offset}
\]
计算，再用 \texttt{pread}/\texttt{pwrite} 执行。\texttt{max\_file\_size} 在启动时传入，默认 64\,MiB。DataServer 内部不维护空闲空间位图、extent 树或块分配器，因为inode 编号本身就是数据文件上的一个逻辑槽位。

这种设计的好处是直接的：数据路径不需要块分配逻辑，寻址是常数时间，服务端状态极简。对本文关注的元数据优化而言还有一项附加好处：数据侧越简单，第~\ref{sec:experiments}~章实验中观察到的性能差异就越容易归到元数据组织、客户端协调和网络通信上，不会被数据块管理细节干扰。

代价也直接。每个 inode 预留 \texttt{max\_file\_size} 大小的逻辑地址空间（默认 64\,MiB），在大量小文件场景下空间利用率偏低。\texttt{delete\_data} 目前为空操作，对象删除后物理空间不会立即回收。稀疏文件能缓解部分空间浪费（实际占用以写入的块数计），但稀疏不是真正的空间管理。这些短板是当前原型有意接受的，为了让论文聚焦在元数据路径上；真实生产场景下需要补上正式的块分配器、空间回收与持久化对象删除。

并发语义上，写入偏移由调用方指定，不同客户端向同一 inode 的不同偏移写入不会因 DataServer 内部地址分配而产生额外竞争；\texttt{pread}/\texttt{pwrite} 以调用为边界完成单次 I/O。若两个写请求本身覆盖了重叠区域，属于上层应用的并发语义问题，不在 DataServer 的责任范围内。

\subsection{RPC 与连接层}
\label{sec:impl_rpc}

gRPC 在这条路径上不只是通信。接口长什么样、一次 RPC 携带多少信息、连接层怎么组织，都会影响客户端与服务端之间的往返次数，进而影响整体性能。本节按接口设计与分层、连接池优化两部分讨论。

\subsubsection{接口设计与分层}

RucksFS 的 gRPC 接口遵循两条原则。

第一条是\textbf{元数据操作与数据操作严格分离}。MetadataServer 的 RPC 接口按职责分成四类：命名空间操作（\texttt{Lookup}、\texttt{Create}、\texttt{Mkdir}、\texttt{Unlink}、\texttt{Rmdir}、\texttt{Rename}、\texttt{Readdir}）、属性操作（\texttt{GetAttr}、\texttt{SetAttr}）、句柄操作（\texttt{Open}、\texttt{Release}、\texttt{CreateAndOpen}）以及写入后的元数据协调（\texttt{ReportWrite}）；DataServer 的接口刻意精简，只包含 \texttt{Read}、\texttt{Write}、\texttt{Truncate}、\texttt{DeleteData}、\texttt{Flush} 五个直接面向字节内容的调用。两套服务定义互不交叉，从接口层面就能清楚谁负责对象语义、谁负责数据字节，客户端对一个请求也一眼就能看出走的是元数据链路还是数据链路。

第二条是\textbf{让一次 RPC 尽量返回下一阶段所需的全部信息}，避免把简单操作拆成多轮往返。具体落点有三处：\texttt{Open} 的响应里既有 \texttt{FileHandle} 又有 \texttt{DataLocation}，客户端不必再发一次“问路由”的调用；\texttt{Unlink} 的响应里携带 \texttt{purged\_inodes}，让客户端直接知道哪些 inode 需要到 DataServer 侧清理；\texttt{SetAttr} 的响应里有 \texttt{needs\_truncate} 指示客户端是否需要补一个截断请求。\texttt{CreateAndOpen} 把这一原则再推进一步。典型的 \texttt{open(O\_CREAT)} 在客户端层面会先 \texttt{Lookup} 判断路径是否存在、若不存在再 \texttt{Create} 插入记录、最后 \texttt{Open} 获取句柄；把这三步合并到一次 RPC 之后，当客户端明确希望”没有则新建、有则直接打开”时，可以减少两次中间往返。

\subsubsection{多通道连接池}

连接层的工程优化点比较集中。tonic 建立的每一个 \texttt{Channel} 对应一条独立的 HTTP/2 连接；HTTP/2 帧在单条 TCP 连接上是顺序序列化的，客户端发出的请求再多，单条连接上写帧这件事本身也只能按顺序完成。本地压测下这会直接成为瓶颈：单条连接的 \texttt{create} 吞吐上限在 34\,000\,ops/s 左右；继续加压，服务端有富余 CPU，但发不出更多请求。

解决办法是让客户端维护多条独立的 \texttt{Channel}，每次 RPC 以轮询方式选一条发送。池的大小由环境变量 \texttt{RUCKSFS\_CLIENT\_POOL\_SIZE} 配置，默认为 1；第~\ref{sec:experiments}~章的高并发测试实际运行时把池大小设到 4 到 8 之间。多通道之间相互独立，帧序列化瓶颈在连接池维度被打散，服务端的多核处理能力才能用上。代价是客户端需要维护一组空闲连接，每条连接各自维持 keepalive。对客户端来说内存与 FD 开销略增，但在分布式文件系统的场景下这些开销可以忽略。

连接层还在内部了 keepalive 调优。tonic 的默认 HTTP/2 超时设置对短连接场景比较合理，但 FUSE 客户端通常一挂就是小时级别；默认配置下偶尔会有连接被服务端侧关闭、客户端下一个请求发现失败再重建的情况。当前实现显式启用了 HTTP/2 keepalive，周期小于空闲超时，避免连接被中间网络设备或对端无感知地关闭。

\subsection{错误处理与恢复}
\label{sec:impl_error}

当前的客户端 + MetadataServer + DataServer 三个组件，需要处理的失败类型有很多：事务冲突、RPC 失败、服务崩溃各自有不同的错误来源，不能一律按重试处理。本节从错误码映射和崩溃恢复两方面阐述当前实现的故障边界。

\subsubsection{错误码映射与重试策略}

第一类是 MetadataServer 内部的事务冲突。这是并发目录操作最常见的失败，即多个事务争夺同一行锁，等待超时后 RocksDB 返回 \texttt{TransactionConflict}。本文没有把它直接抛给用户，而是在服务端先做有限重试：最多 3 次，起始退避 50\,\textmu s、每次翻倍。只有重试仍然失败，才以 \texttt{TransactionConflict} 向上返回，经 gRPC 传到客户端后在 FUSE 层映射为 \texttt{EAGAIN}。这样偶发冲突不会转化为用户可见错误；\texttt{EAGAIN} 的语义是“事务冲突暂未消解”，不与任意 I/O 异常混在一起。

第二类是 RPC 或本地 I/O 失败。当前原型对这类失败的处理相对保守：客户端按 gRPC 返回的错误状态向上报对应的文件系统错误，不在数据路径上维护待确认的写队列，也没有做基于 \texttt{inode + offset} 的幂等重发。真实生产部署下，数据路径的幂等重发、连接重建之后的状态恢复是必须补齐的；由于论文聚焦于元数据路径的验证，这部分工作留作后续。

\subsubsection{服务崩溃的恢复边界}

MetadataServer 崩溃时，RocksDB TransactionDB 的 WAL 提供崩溃一致性：已经提交但尚未刷入 SST 的内容在重启后由 WAL 回放恢复，尚未完成提交的事务不会被当作已经成功。因此只要某次元数据操作已经向客户端返回成功，其持久化结果就不会因 MetadataServer 进程崩溃而丢失。客户端侧会把崩溃感知为一次 RPC 失败或重试；必要时 \texttt{open} 可以重新向 MetadataServer 请求新的 \texttt{DataLocation}。

DataServer 的崩溃语义较简单，也受当前实现边界的约束。若负责某文件的 DataServer 不可达，对应的 \texttt{read} 或 \texttt{write} 直接返回 \texttt{EIO}。当前原型没有数据副本也没有多 DataServer 故障切换，DataServer 不可达不会被透明屏蔽，而是直接暴露为该文件暂时不可访问。

\subsubsection{讨论范围}

本文的讨论范围受若干明确边界约束。当前 RucksFS 保持“单 MetadataServer + 单默认 DataServer”这条主路径。\texttt{DataLocation} 的字段设计和客户端的数据路由都为多 DataServer 预留了接口，但元数据分片、多副本一致性、物理空间回收、严格的双阶段 \texttt{fsync} 都尚未实现。本文的研究重点是元数据面和数据面分离之后，如何通过键编码、事务控制、客户端协调和增量更新来优化元数据关键路径；第~\ref{sec:experiments}~章的实验也是在这条主路径上完成的。多副本容错、跨节点故障转移或空间回收等更大范围的能力不是本文的重点，留给后续研究。

\subsection{本章小结}

本章把第~\ref{sec:design}~章的元数据设计落到一个由 FUSE 客户端、MetadataServer 与 DataServer 三段组成的分布式原型上。

客户端层面，请求按"纯元数据 / 纯数据 / 跨服务"三类从 FUSE 分发到对应后端，\texttt{open}、\texttt{release}、覆盖式 \texttt{rename} 等跨服务语义在客户端侧统一编排，文件句柄中缓存 \texttt{(server\_id, inode)}，使后续 \texttt{read}/\texttt{write} 的稳态路径只走一次 RPC。

MetadataServer 在 tokio 异步接口与 RocksDB 同步事务之间用 \texttt{spawn\_blocking} 搭桥，配合 \texttt{InodeFoldedCache} 的分片锁降低热点争用；启动时固定根 inode 编号与列族布局，崩溃恢复由 RocksDB WAL 保证。DataServer 维持最小实现：\texttt{Read}/\texttt{Write}/\texttt{Truncate}/\texttt{DeleteData}/\texttt{Flush} 五个接口直接落到 \texttt{RawDiskDataStore} 的固定偏移映射上。

跨服务协议层面，MDS 与 DS 接口按元数据与数据严格分离，并让一次 RPC 尽量覆盖下一阶段所需的全部信息：\texttt{Open} 捎带 \texttt{DataLocation}、\texttt{Unlink} 捎带 \texttt{purged\_inodes}、\texttt{SetAttr} 捎带 \texttt{needs\_truncate}，\texttt{CreateAndOpen} 把路径检查、插入、句柄分配合并到单次调用；连接层用多通道连接池绕开 HTTP/2 单连接帧序列化的瓶颈，加上 keepalive 避免长会话下的连接静默关闭。错误处理分三类：事务冲突由服务端最多三次重试消化后以 \texttt{EAGAIN} 暴露；RPC 与本地 I/O 失败按 gRPC 状态向上透传，不做数据路径幂等重发；MDS 崩溃由 WAL 回放恢复，DS 不可达暴露为 \texttt{EIO}。

这一组实现构成了第~\ref{sec:experiments}~章实验的被测系统；多副本、元数据分片、空间回收与严格的双阶段 \texttt{fsync} 不在本文的讨论范围内。
