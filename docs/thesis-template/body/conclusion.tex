\section{总结与展望}

\subsection{工作总结}

本文围绕“基于 KV 存储管理文件系统元数据”这一具体问题，设计并实现了 RucksFS——一个以 RocksDB 为元数据引擎、通过 FUSE 对外提供 POSIX 接口的分布式文件系统原型，并完成了较为完整的实验评估。具体完成的工作如下：

\begin{enumerate}
    \item 实现了基于 RocksDB 多列族的 FUSE 文件系统 RucksFS，支持 create、mkdir、unlink、rmdir、rename、readdir 等常用 POSIX 操作。元数据按访问模式划分到 inodes、dir\_entries、delta\_entries、system 四个列族；目录项键采用“父 inode + 子文件名”的大端序拼接编码，使文件操作基本对应于对目录树上一条边的增删。
    \item 引入了增量更新与懒折叠机制（DeltaOp）。父目录属性的修改不再就地重写基值，而是追加一条 DeltaOp，读路径在读取时再将基值和增量合成；后台线程在增量条数超过 32 时执行折叠。该机制借鉴了 Mantle 的 Delta Record 思路，但实现上更为轻量，整个流程都在单个 RocksDB 实例内完成。
    \item 基于 RocksDB 悲观事务（PCC）处理并发。所有跨键的目录操作通过事务完成加锁与冲突检测；跨 inode 操作按编号排序加锁以避免死锁；临时冲突时服务端自动重试最多 3 次，失败后才向上层返回 EAGAIN。
    \item 客户端基于 fuse3 + tokio 异步实现，挂载时启用 default\_permissions 与 AllowOther，常规权限检查交由内核 VFS 完成。
    \item 在 1 台 64 核服务器加最多 96 台 2 核客户端的腾讯云集群上完成了系统评估。POSIX 合规性方面，将 pjdfstest 398 个用例按实现范围分解后，本文明确不实现的 mknod/mkfifo 相关用例（167 条）、DataServer 单文件大小用例（1 条）和框架自身跳过项（48 条）合计 216 条；剩下 182 条“实现范围内”用例在分布式部署下全部通过，通过率 100\%。性能方面分两段：$N \in \{2, 8, 32\}$ 三档下进行三方对比，RucksFS 相对 JuiceFS+TiKV 在 create/remove/stat 三项上分别领先约 1.5--1.7 倍、1.8--2.2 倍与 1.2 倍；相对 NFS 在 $N=2$ 处因关闭客户端缓存、单次 create 多走两次 RPC 而略低于 NFS，但到 $N=32$ 时 ext4 的 \texttt{i\_rwsem} 锁开始主导，RucksFS 在 create 上反超 NFS 约 4.6 倍；stat 方向 NFS 始终领先 RucksFS，原因是 ext4 B+ 树点查与内核 NFS 路径在读取上更为直接。$N \geq 64$ 时 NFS 和 JuiceFS+TiKV 均无法给出稳定的数据，此时转用 Delta 与 NoDelta 两个构建变体互相对照，沿 $\mathrm{np} = N \times r$ 两个维度扫描——$\mathrm{np}$ 跨过 96 后 NoDelta 进入事务冲突循环、稳态吞吐停在 12\,000\,ops/s 附近，Delta 则继续扩展至 38\,000\,ops/s，$\mathrm{np}=384$ 时 create 比值 2.96、remove 比值 2.63；easy 模式下两者基本一致。
\end{enumerate}

\subsection{不足与展望}

前述数据说明 RucksFS 在元数据关键路径上已经达到了当初设定的目标，但作为毕业设计阶段的原型，系统仍有若干明显不足。下列几项按优先级由高到低列出。

\begin{enumerate}
    \item \textbf{gRPC 网络往返开销}。这是目前最直接的性能短板。分布式模式下，一次 create 需要多次跨节点 RPC，网络延迟占了相当大的比重。后续可以通过合并相邻 RPC 调用、引入客户端元数据缓存等方式加以缓解。
    \item \textbf{DataServer 仍是最小实现}。当前 \texttt{RawDiskDataStore} 使用固定偏移公式直接映射 inode，\texttt{delete\_data} 是空操作，不做空间回收，单文件存在 128\,MiB 的逻辑上限。实验中使用 mdtest 的空文件负载可以跑通，但要作为真实可用的文件系统，还需要补上块分配、空间回收以及对大文件的支持。
    \item \textbf{多节点扩展尚未实现}。当前实现虽然已经将 Client、MetadataServer、DataServer 分开部署，但主实现仍是单 MetadataServer 加单默认 DataServer，多节点元数据分片、数据副本以及故障切换均未实现。对应的架构预留（例如 \texttt{DataLocation} 中的路由字段）已经具备，但仍需后续完善。
    \item \textbf{元数据预取与智能调度}。目前对所有元数据请求一视同仁，未区分冷热路径。实际负载中“readdir 完后立即批量 stat”较为常见，这类序列其实可以提前预取。后续可以探索利用 eBPF 在内核中采集访问模式，并据此实施预取。该方向的工程复杂度较高，建议排在前两项之后。
\end{enumerate}
