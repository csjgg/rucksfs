//! Standalone RPC client binary placeholder.
//!
//! This will be fully implemented after the RPC layer refactor (task 6).

fn main() {
    eprintln!("rucksfs-client: RPC layer not yet refactored. Use the `rucksfs` binary instead.");
    std::process::exit(1);
}
